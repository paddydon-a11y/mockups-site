/* LabourLok site configuration. Edit here, nothing else needs to change. */
window.LABOURLOK = {
  // Form delivery endpoint (Formspree / Web3Forms / own API). Leave blank until connected.
  formEndpoint: "",
  // Dedicated Help & Support number. Leave blank until confirmed; the site shows "to be confirmed".
  supportPhone: "",
  // WhatsApp Business number in international format without "+" (e.g. 447700900000). Blank until confirmed.
  whatsapp: "",
  // General enquiries mailbox shown on the contact page.
  email: "hello@labourlok.co.uk",
  // Staffed support hours (24h clock, Europe/London). Days: 0 = Sunday.
  staffed: { days: [1, 2, 3, 4, 5], from: 8, to: 18 },
  // Google Analytics 4 measurement ID. Leave blank to load nothing (no cookies set).
  gaId: ""
};
